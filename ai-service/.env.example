# Example environment variables for AI Service (FastAPI)

LOG_LEVEL=INFO
# OPENAI_API_KEY=your_openai_api_key_here
# DATABASE_URL=postgresql://user:password@host:port/db_ai_service # If direct DB access is needed
# MODEL_PATH=/app/models/your_model_file # Example if loading a local model
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/gcp-credentials.json # For GCP services like Vertex AI, Cloud Storage